{
  "name": "medimesh-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "MediMesh Healthcare Platform — Monorepo Root (CI/CD tooling only)",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit",
    "lint-staged": "lint-staged"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.7",
    "eslint": "^8.56.0"
  }
}
